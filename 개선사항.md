# comorph 0.3.1 적용 후 개선 사항

`migration-runners/vitest-3-to-4`에 `comorph@0.3.1`을 적용하면서 확인한 상태와 남은 과제를 정리한다.

## 0.3.1에서 적용된 개선

이번 버전에서 runner 전환에 직접 도움이 된 API는 다음이다.

- `ObjectEditor.set()`의 missing parent object 생성
- `ObjectEditor.takeEntries()`
- `ObjectEditor.set()`의 builder value 타입 호환
- `ImportManager.findNamed()`
- import binding의 `references()`, `renameImported()`, `moveToSource()`
- `arrayElements.replaceAll()`
- `file.insertAfterImports()`, `file.pickLocalName()`
- read-only `querySource()`

## 이번에 runner에 반영한 전환

아래 파일은 jscodeshift transform을 제거하거나 comorph 기반으로 더 많이 전환했다.

- `config-changes/module-runner.ts`
  - `test.deps.*` -> `test.server.deps.*` 이동에 `ObjectEditor.set()`의 parent 생성 사용
  - `optimizer.web` -> `optimizer.client` rename은 별도 comorph transform으로 분리
  - 한 transaction에서 rename과 sibling move를 같이 하면 아직 `patch.overlap`이 나므로 순차 실행으로 우회
- `config-changes/browser-provider.ts`
  - `takeEntries()`와 `objectValue`/`arrayValue`로 `browser.instances` 재구성
- `config-changes/reporter-updates.ts`
  - `arrayElements.replaceAll()`과 builder로 reporter 배열 재작성
- `api-changes/update-deprecated-type-imports.ts`
  - `imports().findNamed()`와 binding references로 deprecated type import 및 참조 rename 처리

## 아직 직접 jscodeshift transform으로 남은 파일

### `config-changes/coverage-options.ts`

남은 이유:

- `comorph@0.3.1`에서도 아래 연속 제거가 `patch.comment-sensitive`로 review 처리된다.

```ts
config.remove("test.coverage.all");
config.remove("test.coverage.extensions");
config.remove("test.coverage.ignoreEmptyLines");
config.remove("test.coverage.experimentalAstAwareRemapping");
```

- 실제 주석이 없는 테스트 케이스에서도 실패한다.
- 기존 migration 동작을 유지하려면 jscodeshift fallback이 필요하다.

필요한 comorph 개선:

- 연속 object property removal의 comment-sensitive 오탐 제거
- 같은 object 내 여러 removal을 final-state patch로 병합
- property 단위 comment range 판정

### `api-changes/update-browser-utils-imports.ts`

남은 이유:

- `file.insertAfterImports()`는 생겼지만 이 변환에는 import declaration 전체 제거와 namespace import 제거가 필요하다.
- 현재 comorph ImportManager에는 다음 API가 아직 부족하다.

필요한 comorph 개선:

- `imports().removeNamespace(source)`
- `imports().removeDefault(source)`
- `imports().findNamespace(source)`
- `imports().findDefault(source)`
- import declaration 전체 제거 API
- named import와 namespace import를 한 migration에서 안전하게 다루는 helper

## read-only querySource 전환 상태

config blocker 3개는 `querySource()` 기반으로 옮겼다.

- `config-changes/module-runner.ts`
- `config-changes/browser-provider.ts`
- `config-changes/pool-rework.ts`

이를 위해 `utils/comorph-query.ts`를 추가했다. 이 유틸은 `objectProperty` query 결과의 parent chain에서 static object path를 계산한다.

아직 jscodeshift AST scan이 남아 있는 review scanner는 다음이다.

- `api-changes/review-source-api-changes.ts`

이 파일의 `vi.mock` factory 분석은 Program AST 전체 traversal, top-level binding 수집, nested function scope 분석이 필요하다. 현재 `querySource()`는 pattern query 중심이라 같은 의미로 대체하기 어렵다.

필요한 추가 개선:

- object property의 static path를 쉽게 확인하는 helper
- `objectProperty` match에서 parent object path 조회
- scanner finding을 `BlockCheckResult`로 바꾸는 runner helper

## 다음 우선순위

1. `coverage-options.ts`를 막는 comment-sensitive 오탐 해결
2. `update-browser-utils-imports.ts`에 필요한 namespace/default import 제거 API 추가
3. `review-source-api-changes.ts`를 옮길 수 있도록 read-only Program traversal/query 보강
4. `module-runner.ts`의 두 comorph transform을 하나의 transaction으로 합칠 수 있게 patch planner 개선
