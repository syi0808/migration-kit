name: test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  vitest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: VITEST_MAX_THREADS=2 VITE_NODE_DEPS_MODULE_DIRECTORIES=./test pnpm test
